
import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#210f33',
        },
        secondary: {
            main: '#ffffff',
        },
        background: {
            default: '#171515',
            white: '#ffffff'
        }
    },
})

export default theme